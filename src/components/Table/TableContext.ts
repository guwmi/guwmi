/**
 * Table context for the table component *************************************************************
 */

import { createContext } from 'react';
const TableContext = createContext(null);
export default TableContext;