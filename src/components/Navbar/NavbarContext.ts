/**
 * Navbar context for the navbar component *****************************************************
 */

import { createContext } from 'react';
const NavbarContext = createContext(null);
export default NavbarContext;